📘 Explicação Didática do Código
🔹 Estrutura básica
public class Student extends Person {


A classe Student herda de Person (por isso usamos extends).

Isso significa que ela vai ter tudo o que Person tem (atributos name, age, email) + os atributos específicos de aluno.

🔹 Atributos do Student
private String varRegistrationNumber;
private String varCourse;
private double[] gradesArray;
private int varYear;
private int varAttendancePercentage;


varRegistrationNumber → matrícula do aluno.

varCourse → curso ou série.

gradesArray → notas do aluno em formato de array (double[]).

varYear → ano letivo.

varAttendancePercentage → presença do aluno em % (ex: 80 significa 80%).

📌 Todos os nomes seguem a regra que você pediu: prefixo var em variáveis e Array no caso das notas.

🔹 Construtores (formas de criar um aluno)

Um construtor é como o “molde” que usamos para criar um objeto.

1️⃣ Criar apenas com matrícula:

public Student(String varRegistrationNumber) {
    super(null, 0, null);
    this.varRegistrationNumber = varRegistrationNumber;
}


super(null, 0, null) → chama o construtor de Person (mesmo que name, age, email fiquem nulos ou 0).

Define apenas a matrícula.

2️⃣ Criar com matrícula + curso:

public Student(String varRegistrationNumber, String varCourse) {
    super(null, 0, null);
    this.varRegistrationNumber = varRegistrationNumber;
    this.varCourse = varCourse;
}


Aqui além da matrícula, já sabemos o curso.

3️⃣ Criar com todos os dados:

public Student(String varName, int varAge, String varEmail,
               String varRegistrationNumber, String varCourse) {
    super(varName, varAge, varEmail); // chama o Person
    this.varRegistrationNumber = varRegistrationNumber;
    this.varCourse = varCourse;
}


Aqui usamos o super corretamente para inicializar os atributos herdados de Person.

Esse é o construtor mais completo.

🔹 Getters e Setters (com encadeamento)

Exemplo:

public String getCourse() { return varCourse; }
public Student setCourse(String varCourse) {
    this.varCourse = varCourse;
    return this; // permite chaining
}


O getCourse retorna o valor da variável.

O setCourse atualiza o valor e retorna this.

Retornar this permite escrever:

student.setCourse("Math").setYear(2024).setAttendancePercentage(90);


👉 Isso é method chaining (chamadas encadeadas).

🔹 Métodos herdados da Person
@Override public String getRoleDescription() { return "Student"; }
@Override public String getUniqueIdentifier() { return varRegistrationNumber; }


getRoleDescription → sempre retorna "Student" porque essa é a função da pessoa na instituição.

getUniqueIdentifier → aqui o identificador único do aluno é a matrícula.

🔹 8 métodos obrigatórios

Agora os métodos que fazem a lógica do aluno:

Média das notas

public double calculateAverageGrade() {
    if (gradesArray == null || gradesArray.length == 0) return 0;
    double sum = 0;
    for (double g : gradesArray) sum += g;
    return sum / gradesArray.length;
}


Se não tiver notas, retorna 0.

Senão, soma todas e divide pelo tamanho do array.

Aprovado?

public boolean hasPassed() { return calculateAverageGrade() >= 6; }


Se a média for 6 ou mais, retorna true (aprovado).

Tem presença mínima?

public boolean hasMinimumAttendance() { return varAttendancePercentage >= 75; }


Retorna true se a presença for 75% ou mais.

Iniciais do nome

public String getInitials() {
    if (getName() == null) return "";
    String[] parts = getName().split(" ");
    String ini = "";
    for (String p : parts) ini += p.charAt(0);
    return ini.toUpperCase();
}


Pega o nome do aluno, separa em palavras, e junta só as primeiras letras.

Exemplo: "Ana Maria Souza" → "AMS".

Email formatado

public String getFormattedEmail() {
    return getEmail() == null ? "" : getEmail().toLowerCase().trim();
}


Transforma em minúsculo e remove espaços extras.

Status acadêmico

public String getAcademicStatus() {
    return varCourse + " - Year " + varYear +
           " | Avg " + calculateAverageGrade() +
           " | Att " + varAttendancePercentage + "%";
}


Monta uma frase com curso, ano, média e presença.

Exemplo: "Math - Year 2024 | Avg 7.5 | Att 80%".

📌 Resumo

Student herda de Person.

Tem atributos específicos de aluno (matrícula, curso, notas, ano e presença).

Construtores diferentes para criar objetos de várias formas.

Getters e Setters usam method chaining.

Implementa métodos obrigatórios (getRoleDescription, getUniqueIdentifier).

Implementa 8 métodos extras que lidam com notas, presença e informações do aluno.
